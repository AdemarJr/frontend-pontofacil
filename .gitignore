# Credenciais e ambiente — nunca commitar (exceto .env.example)
# CRA embute REACT_APP_* no build; variáveis reais ficam no Vercel/Railway.
.env
.env.*
.env-*
hom.env
hom.env.*
*.env.local
!.env.example
*.pem
*.key
secrets/
credentials.json

# Dependências
node_modules/

# Build de produção (npm run build)
build/

# Cache do webpack / eslint (CRA)
.cache/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testes
coverage/

# SO / editor
.DS_Store
.idea/
*.swp
*~

# Opcional: descomente se usar TypeScript local
# *.tsbuildinfo
